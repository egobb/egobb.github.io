name: Issue 39 baseline refresh (inactive)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inactive:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Baseline refresh completed; this verification-only workflow is intentionally inactive."
