source "https://rubygems.org"

# Keep versions flexible for GitHub Pages
gem "github-pages", group: :jekyll_plugins

# Theme used via remote_theme (no direct gem needed), but we include jekyll-include-cache if you ever enable it.
gem "jekyll-include-cache"